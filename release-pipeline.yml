trigger:
  - none

resources:
  pipelines:
    - pipeline: fading-flame.build  # Name of the pipeline resource
      source: fading-flame.build # Name of the triggering pipeline
      trigger:
        branches:
          - master

variables:
  vmImageName: 'ubuntu-20.04'

jobs:
  - deployment: Deploy
    displayName: Deploy
    environment: dev
    strategy:
      runOnce:
        deploy:
          steps:
            - download: fading-flame
              artifact: drop
              
            - task: CopyFilesOverSSH@0
              displayName: 'Copy compose file'
              inputs:
                sshEndpoint: HetziPrivate
                sourceFolder: '$(System.DefaultWorkingDirectory)'
                targetFolder: '/usr/local/release-compose-files/fading-flame/test'
                flattenFolders: true
                
            - task: SSH@0
              displayName: 'Run shell inline on remote machine'
              inputs:
                sshEndpoint: HetziPrivate
                runOptions: inline
                inline: |
                  cd /usr/local/release-compose-files/fading-flame/test
                  MONGO_CONNECTION_STRING=mongodb://157.90.1.251:3513 docker-compose -p $(Release.DefinitionName)-$(CONTAINER_SUFFIX) pull 
                  MONGO_CONNECTION_STRING=mongodb://157.90.1.251:3513 docker-compose -p $(Release.DefinitionName)-$(CONTAINER_SUFFIX) up -d 
                failOnStdErr: false  